import { PlaceholderScreen } from "@/components/layout/PlaceholderScreen";

export default function AdminRolesPage() {
  return (
    <PlaceholderScreen
      title="Roles & Permissions"
      description="Read-only matrix for the demo. Editing is out of scope."
    />
  );
}
