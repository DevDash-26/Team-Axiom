import { PlaceholderScreen } from "@/components/layout/PlaceholderScreen";

export default function BookingsPage() {
  return (
    <PlaceholderScreen
      title="Find a Classroom"
      description="Search availability by date and time, then submit a request."
    />
  );
}
