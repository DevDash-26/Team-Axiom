import { PlaceholderScreen } from "@/components/layout/PlaceholderScreen";

export default function StaffRequestsPage() {
  return (
    <PlaceholderScreen
      title="Student Requests"
      description="Academic support, facility issues, and feedback in one queue."
    />
  );
}
