import { PlaceholderScreen } from "@/components/layout/PlaceholderScreen";

export default function StaffBookingsPage() {
  return (
    <PlaceholderScreen
      title="Room Requests"
      description="Approve or reject booking requests. Rejection will require a reason."
    />
  );
}
